version: '3'

services:
    stockapi:
        build:
            context: .
            dockerfile: Dockerfile
        image: node:latest
        container_name: stockapi
        restart: unless-stopped
        ports:
            - '3333:3333'
        volumes:
            - .:/home/node/stockapi
